import { Demo } from '@components/Demo'

## Webchat Advance Components

The webchat offers 4 advanced components to help you build a more advanced version of the webchat. These components are:

### Header

![webchat-embed-config](/docs/content/webchat/component-header.png)

```jsx /Header/
import "./style.css"
import theme from "./theme.json"

import { Header, Container, WebchatProvider, useClient } from '@botpress/webchat'

const App = () => {
  const client = useClient({ clientId: '453254325-54325-435-345-345534253' })

  return (
    <WebchatProvider client={client} theme={theme}>
      <Container>
        <Header />
      </Container>
    </WebchatProvider>
  )
}
```

<Demo src="https://stackblitz.com/edit/bp-webchat-template-c51e4w?embed=1&file=src%2FApp.tsx&hideNavigation=1" title="Header Demo" />

### MessageList

![webchat-embed-config](/docs/content/webchat/component-message-list.png)

```jsx /MessageList/
import "./style.css"
import theme from "./theme.json"

import { MessageList, Container, WebchatProvider, useClient } from '@botpress/webchat'

const App = () => {
  const client = useClient({ clientId: '453254325-54325-435-345-345534253' })

  return (
    <WebchatProvider client={client} theme={theme}>
      <Container>
        <MessageList />
      </Container>
    </WebchatProvider>
  )
}
```

<Demo src="https://stackblitz.com/edit/bp-webchat-template-c51e4w?embed=1&file=src%2FApp.tsx&hideNavigation=1" title="Message List Demo" />

### Composer

![webchat-embed-config](/docs/content/webchat/component-composer.png)

```jsx /Composer/
import "./style.css"
import theme from "./theme.json"

import { Composer, Container, WebchatProvider, useClient } from '@botpress/webchat'

const App = () => {
  const client = useClient({ clientId: '453254325-54325-435-345-345534253' })

  return (
    <WebchatProvider client={client} theme={theme}>
      <Container>
        <Composer />
      </Container>
    </WebchatProvider>
  )
}
```

<Demo src="https://stackblitz.com/edit/bp-webchat-template-c51e4w?embed=1&file=src%2FApp.tsx&hideNavigation=1" title="Composer Demo" />

### Fab

![webchat-embed-config](/docs/content/webchat/component-fab.png)

```jsx /Fab/
import "./style.css"
import theme from "./theme.json"

import { Fab, Container, WebchatProvider, useClient } from '@botpress/webchat'

const App = () => {
  const client = useClient({ clientId: '453254325-54325-435-345-345534253' })

  return (
    <WebchatProvider client={client} theme={theme}>
      <Container>
        <Fab />
      </Container>
    </WebchatProvider>
  )
}
```

<Demo src="https://stackblitz.com/edit/bp-webchat-template-c51e4w?embed=1&file=src%2FApp.tsx&hideNavigation=1" title="Fab Demo" />
